1 + 80; //=> 81
60 - 40; //=> 20
2 * 3.4; //=> 6.8
5.0 / 2.5; //=> 2
function add() {
  // we'll fill this in shortly
}
function subtract() {
  // we'll fill this in shortly
}
function multiply() {
  // we'll fill this in shortly
}
function divide() {
  // we'll fill this in shortly
}
// Write your functions here:
function add(num1, num2) {
  return num1 + num2;
}
function subtract(num1, num2) {
  return num1 - num2;
}
function multiply(num1, num2) {
  return num1 * num2;
}
function divide(num1, num2) {
  return num1 / num2;
}
function add(a, b) {
  return a + b;
}
function increment(n) {
  return n + 1;
}

// decrement.js  
function decrement(n) {
  return n - 1;
}
function makeInt(n) {
  return parseInt(n, 10); // Base 10 parsing
}
function preserveDecimal(n) {
  return parseFloat(n);
}